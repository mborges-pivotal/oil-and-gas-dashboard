# Dependencies
node_modules/

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Local config overrides (keep config.json defaults, ignore personal copies)
oilgas-config.json

# Local API keys / secrets (see .env.example) — never commit real values
.env

# Logs
*.log
